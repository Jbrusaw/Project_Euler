#ifndef FUNCTIONS_H
#define FUNCTIONS_H

char is_prime(const int);
char is_prime_with_sieve(const int, const int*, const int);
int* prime_sieve_under(const int);
int* prime_sieve_first(const int);

#endif
